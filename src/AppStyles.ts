import {CSSProperties} from "react";

const AppStyles: Record<string, CSSProperties> = {
  alignRow: {
    flexDirection: 'row',
    flex: 1,
    alignItems: 'center',
  }
}

export default AppStyles
